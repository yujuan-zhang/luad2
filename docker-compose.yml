name: luadtx

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: luadtx
      POSTGRES_PASSWORD: luadtx_dev
      POSTGRES_DB: luadtx
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
