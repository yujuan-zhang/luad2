server {
    listen 80;
    server_name luadtx.stoichioomics.com;

    # Let's Encrypt HTTP-01 challenge files land here.
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name luadtx.stoichioomics.com;

    ssl_certificate /etc/letsencrypt/live/luadtx.stoichioomics.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/luadtx.stoichioomics.com/privkey.pem;

    location /api/ {
        proxy_pass http://backend:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Streamlit needs the websocket upgrade headers for its live UI updates.
    location / {
        proxy_pass http://frontend:8501/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400;
    }
}
